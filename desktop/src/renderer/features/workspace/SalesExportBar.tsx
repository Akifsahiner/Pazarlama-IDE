import { OutreachDispatchPanel } from "@renderer/features/assets/OutreachDispatchPanel";

/** Compact workspace bar — delegates to OutreachDispatchPanel (Faz 10). */
export function SalesExportBar() {
  return <OutreachDispatchPanel variant="bar" />;
}
